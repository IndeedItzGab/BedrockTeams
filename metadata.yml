name: BedrockTeams
version: V1.1.7
status: STABLE
message: |
  # BedrockTeams V1.1.7
  - In-game configuration vai user interface. Therefore, the file configuration is now removed.
  - Added slash commands cooldown.
  - Some adjustment with the user interface.
  - Fixed issue where wrong team were selected on the user interface (reported by @starwarguy)
  - Fixed issue where a team could be created regardless of limitation through user interface.