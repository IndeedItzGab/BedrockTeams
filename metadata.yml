name: BedrockTeams
version: V1.1.6
status: STABLE
message: |
  # BedrockTeams V1.1.6
  - Improvement with cancelling damages
  - Improvement with checking pass combat
  - Fixed players were allowed to use `/team warp` and `/team home` during combat